# 数据库配置
spring.datasource.url=jdbc:mysql://localhost:3306/secondhand?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=20050508
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA配置
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL57Dialect

# 服务器配置
server.port=8080

# 跨域配置
spring.mvc.cors.allowed-origins=http://localhost:8081
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.mvc.cors.allowed-headers=*
spring.mvc.cors.allow-credentials=true
# application.properties
spring.mvc.pathmatch.matching-strategy=ant_path_matcher

# JWT配置
jwt.secret=secondhand_secret_key
jwt.expiration=3600000

# 日志配置
logging.level.root=INFO
logging.level.com.school.secondhand=DEBUG
logging.level.org.springframework.web=DEBUG

file.upload-dir=src/main/app-uploads/